"""homestead_ledger.app — S1. A window. Thin over the core (I-29).

The tkinter view itself is bite 3; this bite builds only the display-free
state machine (`window.py`) and the headless `--demo` composition
(`demo.py`), the same split homestead-law made between its own `window.py`
and `view.py`.
"""
